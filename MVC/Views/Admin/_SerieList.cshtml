@model IEnumerable<Business.DTO.NameViewModel>
<div class="spacer"></div>
<p>
    @Ajax.ActionLink("Lägg till serie", "AddSerie", "Admin", null, new AjaxOptions
    {
        UpdateTargetId = "target",
        InsertionMode = InsertionMode.Replace,
        LoadingElementId = "loaderDiv",
        OnFailure = "showXHRError"
    }, new {id = "AddSeries"})
</p>
<table class="table-condensed table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Ajax.ActionLink("Ändra", "EditSerie", "Admin", new {id = item.Id}, new AjaxOptions
                {
                    UpdateTargetId = "target",
                    InsertionMode = InsertionMode.Replace,
                    LoadingElementId = "loaderDiv",
                    OnFailure = "showXHRError"
                }, new {id = "EditSeries"})

                |

                @Ajax.ActionLink("Radera", "DeleteSerie", "Admin", new {id = item.Id}, new AjaxOptions
                {
                    UpdateTargetId = "target",
                    InsertionMode = InsertionMode.Replace,
                    LoadingElementId = "loaderDiv",
                    OnFailure = "showXHRError"
                }, new {id = "DeleteSeries"})
            </td>
        </tr>
    }
</table>