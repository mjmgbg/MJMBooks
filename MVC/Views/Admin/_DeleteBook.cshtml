@model Business.DTO.BookViewModel

<h3>Är du säker på att du vill radera boken?</h3>
<div>
    <h4>Radera boken</h4>
    <hr/>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>
    </dl>
    @using (Ajax.BeginForm("DeleteBook", "Admin", null, new AjaxOptions {UpdateTargetId = "target", HttpMethod = "POST", LoadingElementId = "loaderDiv"}, new {role = "form", id = "frmDeleteBook"}))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Radera" class="btn btn-default"/> |
            @Ajax.ActionLink("Tillbaka", "BookList", "Admin", null, new AjaxOptions
            {
                UpdateTargetId = "target",
                InsertionMode = InsertionMode.Replace,
                LoadingElementId = "loaderDiv",
                OnFailure = "showXHRError"
            }, new {id = "Back"})
        </div>
    }
</div>